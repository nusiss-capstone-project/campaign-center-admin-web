/* Hand-maintained from reward-ms swagger. */
export type data_BudgetVO = {
  available_amount?: string;
  issued_amount?: string;
  total_amount?: string;
  unit?: string;
  voucher_type?: string;
};
